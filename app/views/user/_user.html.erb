<%= will_paginate @users %>
<ul class="users">
  <% @users.each do |user| %>
      <li>
        <%= gravatar_for user %>
        <%= user.name %>
      </li>
  <% end %>
</ul>
<%= will_paginate @users %>
<%=  link_to "XML", "/users.xml" %>